[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "nickel_111"
requires-python = ">=3.7"
version = "0.0.1"
dependencies = [
    "numpy","matplotlib","scipy"
]

